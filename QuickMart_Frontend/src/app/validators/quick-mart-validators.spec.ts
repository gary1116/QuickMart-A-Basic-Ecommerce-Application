import { QuickMartValidators } from './quick-mart-validators';

describe('QuickMartValidators', () => {
  it('should create an instance', () => {
    expect(new QuickMartValidators()).toBeTruthy();
  });
});
